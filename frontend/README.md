# Frontend (Next.js)

## Setup

```bash
cd frontend
npm install
cp .env.example .env.local
```

## Run

```bash
npm run dev
```

Open `http://localhost:3000`.

The frontend expects backend API at `NEXT_PUBLIC_API_BASE`.
